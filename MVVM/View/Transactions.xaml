<UserControl x:Class="ZTP_WPF_Project.MVVM.View.Transactions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZTP_WPF_Project.MVVM.View"
             xmlns:viewmodel="clr-namespace:ZTP_WPF_Project.MVVM.ViewModel" 
             xmlns:core="clr-namespace:ZTP_WPF_Project.MVVM.Core"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel>
            <Button Content="Add" Command="{Binding AddCommand}" CommandParameter="{Binding SelectedTransaction}" />
            <Button Content="Remove" Command="{Binding RemoveCommand}" CommandParameter="{Binding SelectedTransaction}" />
            <Button Content="Edit" Command="{Binding EditCommand}" CommandParameter="{Binding SelectedTransaction}" />
            <Button Content="Undo" Command="{Binding UndoCommand}" />
            <Button Content="Redo" Command="{Binding RedoCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
